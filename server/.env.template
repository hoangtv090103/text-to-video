# Text-to-Video Service Environment Configuration
# Copy this file to .env and fill in your actual values

# ==============================================
# REDIS CONFIGURATION
# ==============================================
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0

# ==============================================
# TTS SERVICE CONFIGURATION
# ==============================================
TTS_SERVICE_URL=http://localhost:4123/v1/audio/speech

# ==============================================
# LLM PROVIDER CONFIGURATION
# ==============================================
# Provider options: openai, google, anthropic, huggingface, local
LLM_PROVIDER=openai

# OpenAI Configuration
OPENAI_API_KEY=sk-dev-replace-with-your-key
OPENAI_MODEL=gpt-3.5-turbo

# Google Gemini Configuration (if using google provider)
GOOGLE_API_KEY=
GOOGLE_MODEL=gemini-2.0-flash

# Anthropic Configuration (if using anthropic provider)
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-3-5-sonnet

# ==============================================
# VISUAL GENERATION CONFIGURATION
# ==============================================
PRESENTON_BASE_URL=http://localhost:5001

# Visual Quality Settings
# DPI for generated images (150 for high quality, 300 for print quality)
IMAGE_DPI=150
IMAGE_QUALITY=95  # JPEG/PNG quality (0-100)
SLIDE_WIDTH=1920  # Standard 1080p width (16:9 aspect ratio)
SLIDE_HEIGHT=1080  # Standard 1080p height
CHART_DPI=150  # DPI specifically for charts/graphs
CODE_DPI=150  # DPI specifically for code screenshots

# ==============================================
# STORAGE CONFIGURATION
# ==============================================
# Backend options: local, s3
ASSET_STORAGE_BACKEND=local

# S3/MinIO Configuration (if using s3 backend)
S3_BUCKET=
S3_REGION=us-east-1
S3_ACCESS_KEY=
S3_SECRET_KEY=

# ==============================================
# CACHING CONFIGURATION
# ==============================================
CACHE_LLM_TTL=3600       # 1 hour in seconds
CACHE_TTS_TTL=86400      # 24 hours in seconds
CACHE_VISUAL_TTL=86400   # 24 hours in seconds

# ==============================================
# JOB & RESOURCE LIMITS
# ==============================================
JOB_RETENTION_HOURS=24
MAX_UPLOAD_SIZE_MB=50
MAX_CONCURRENT_SCENES=10

# ==============================================
# LOGGING CONFIGURATION
# ==============================================
LOG_LEVEL=INFO

# ==============================================
# SETUP INSTRUCTIONS
# ==============================================
#
# 1. REQUIRED SERVICES:
#    - TTS Service: Start Chatterbox TTS on port 4123
#    - Presenton: Start Presenton API on port 5001 (or use remote URL)
#    - Redis: Start Redis server on port 6379
#
# 2. LLM PROVIDER SETUP:
#    - OpenAI: Get API key from https://platform.openai.com/api-keys
#    - Google: Get API key from https://ai.google.dev/
#    - Anthropic: Get API key from https://console.anthropic.com/
#
# 3. S3 SETUP (Optional, Production):
#    - Create S3 bucket or MinIO instance
#    - Generate IAM credentials with S3 permissions
#    - Set ASSET_STORAGE_BACKEND=s3
#    - Fill in S3_* variables
#
# 4. DEVELOPMENT WORKFLOW:
#    - Copy: cp .env.template .env
#    - Edit .env with your actual credentials
#    - Run: uvicorn app.main:app --reload
#
# ==============================================
# SECURITY NOTES
# ==============================================
#
# - NEVER commit .env with real credentials to version control
# - .env is in .gitignore by default
# - Use environment-specific .env files for staging/prod
# - Rotate API keys quarterly
# - Use AWS Secrets Manager or Vault in production
#
