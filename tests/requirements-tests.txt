# Testing Suite Requirements
# Install with: pip install -r requirements-tests.txt

# Core testing framework
pytest>=7.4.0
pytest-asyncio>=0.21.0

# System monitoring
psutil>=5.9.0

# Required dependencies from main app (for imports to work)
pydantic>=2.0.0
pydantic-settings>=2.0.0
fastapi>=0.100.0
httpx>=0.24.0
langchain-google-genai>=0.0.11
aiofiles>=23.0.0
redis>=5.0.0

# Additional utilities
# (Most dependencies should already be installed from main requirements.txt)
